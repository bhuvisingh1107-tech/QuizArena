/**
 * @deprecated Import from `@/lib/app-url` instead.
 * Re-exports kept so existing imports keep working.
 */
export {
  extractDisplaySecretToken,
  getAppOrigin,
  getDisplayPageUrl,
  getJoinPageUrl,
  getQrJoinUrl,
} from '@/lib/app-url'
